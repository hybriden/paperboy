# Shipwright retrospective (append-only; read by auto-plan)

## 2026-08-29 — standalone block preview — COMPLETE (medium, 3 tasks + 2 review cycles)
- Went well: zero-API design held (existing getById + pbt pass-through); the security property (gate before fetch) got the strongest pin available (throwing mock + not-called assert); react-doctor caught a real render-time ref mutation the reviews hadn't yet.
- PLAN PREMISE WRONG, caught in review: the plan claimed standalone preview could keep bridge focus ("the framed doc IS the edited doc") — but the DOM contract wraps the block as pseudo-index 0, so existing machinery would misroute into a Form's first question. Lesson: any claim about bridge message routing must be traced through ATTR/describe() in packages/preview before it enters a plan.
- apps/web facts for future runs: PAPERBOY_API_URL is the BARE origin (the client appends /api/v1/delivery — the e2e-debugging memory said otherwise and was stale); vitest include is app/**; route pages are unit-testable by calling the async component with mocked next/headers+navigation; AGENTS.md means clone in-repo Next conventions, never memory.
- Env: TaskStop leaves Next dev servers listening (EADDRINUSE) — always Get-NetTCPConnection+taskkill the port after stopping (now bitten by web too, not just vite/api).
- The "Settings is tabbed" serial flake appeared again and passed solo (rate-limit contention class; second sighting).

## 2026-08-28 — admin forms builder — COMPLETE (medium, 4 tasks + 2 review cycles)
- Went well: grounding the plan in FORMS_PLAN §9's REASONING (not just its verdict) produced the right architecture (specialized rendering, zero storage change); reusing delivery's own compilers (fieldSpecFromBlock/parseChoices) made the preview drift-proof and reviews fast to verify.
- Went wrong, twice-shaped lesson: "suite green" claims must name the FULL suite — a subset run missed two stale pre-existing forms tests (spec review caught it). And never run e2e while a reviewer subagent also runs e2e: shared-rate-limit contention faked 3 failures (documented gotcha, self-inflicted).
- Env facts moved: 6 preview-bridge e2e (not 3) can't pass on the native local stack — 3 newly baselined against clean main; the api server must RESTART to pick up packages/shared changes (tsx caches at boot) or tests read stale templates.
- Migration lesson: any jsonb_set over an aggregate needs the empty-set guard (jsonb_agg over zero rows is NULL, jsonb_set is strict) — reviewer caught a boot-blocking edge; prove guards in a rolled-back transaction.
- Plan numbering: check the migrations dir at plan time (plan said 0025; 0025 existed).

## 2026-08-28 — admin mobile dashboard/settings — COMPLETE (small, 2 planned tasks + 1 amendment)
- Went well: map lens nailed the core defect (settings w-56 rail) before any code; mount-time hash contract documented at plan time prevented a wrong drill design; TDD red→green on both real defects (drill, 8px top-bar pan).
- Went wrong: the map cleared the Shell as "already mobile-aware" without measuring it — E2E found the top bar panning every phone view 8px. Lesson: an "already handled" claim in a map is load-bearing only after one measurement (a viewport probe costs seconds).
- Plan quality: two sub-criteria (row height, chevron) had no planned assertion — when a criterion names a size or an element, the plan's test sketch must assert it or explicitly waive it.
- Env quirk: `pnpm exec playwright` prints a spurious "Command not found" after FAILING runs on this box — read the test summary line, not the last line.
- Deferred follow-up: focus management on the settings drill swap (focus drops to body on list⇄panel; ref-focus the incoming heading). Minor, axe-silent.

## 2026-08-29 — on-page content-area visibility + add-block (medium, 5 tasks)
Status: COMPLETE. Bridge 0.4.0 (dotted area outlines, hover name tag, "＋ Add block" chip,
additive paperboy:add-block) + admin anchored palette reusing lib/area-add + SharedBlockPicker.
- Red-first worked at every layer (protocol 1 red, bridge 4 red, admin lib 1 red).
- Review caught a REAL fidelity bug the tests couldn't: new fixed-position chrome inside a
  preview creates a DROP DEAD-ZONE for the existing drag path (chip in <body> ⇒
  closest([data-pb-area]) misses ⇒ payload silently dropped). LESSON: when adding overlay
  chrome to a surface that already has pointer-driven features, enumerate the OTHER features'
  hit-testing and hide the chrome for their duration.
- CSS injected as one string: rule ORDER is the only tiebreak at equal specificity — append
  new rules deliberately, not at the end by default.
- `git add -A` after a Playwright evidence run committed 369KB of root PNGs; amend caught it
  pre-push. LESSON: check `git status` before -A when a browser session wrote to the repo root.
- The native e2e preview class is CURED by setting site.preview_base_url in the local DB —
  all preview tests now pass natively (memory updated); no more "3 known-red" carve-out.
- Known limitation accepted: the add palette's anchor doesn't re-track on preview scroll
  (the bridge streams rects only for clicked elements). Same as SharedBlockPicker; revisit
  only if editors report it.

## 2026-08-31 — full-application auto-review — COMPLETE (large: 6 spec/fidelity reviewers, 4 quality reviewers, 6 fix bundles, 7 re-reviews)
- Scope discipline that worked: CLAUDE.md as the spec, partitioned by section; every reviewer had to cite file:line and
  the pinning test, and every fix bundle had to show the RED assertion. Two CRITICALs were only findable by fidelity
  (code vs test vs spec three-way): shared form field blocks dropped from `content.form`, and the delivery richtext branch
  accepting arrays. Neither had a test that could fail.
- The whole-tree review found a class the diff reviews never see: comments claiming a fix ("closes DNS rebinding", "the cv
  bump fixed list ETags") that the code did not deliver. Rule: a comment that claims a guarantee must name the test that pins it.
- Single-runner test DB dictated the shape: DB-touching bundles ran SERIALLY (A-i → A-ii → A-iii → A-iv), admin/web bundles
  in parallel; reviewers forbidden from running DB tests. Cost: ~4 h wall-clock; benefit: zero corrupted-DB false failures.
- Run-lists must be derived by grepping IMPORTS of the changed module, not by feature name — `agent-merge.test.ts` (calls
  `TOOLS[].run` directly) was missed by A-ii and only the full-suite gate caught the new required `run` state.
- Deliberate deviations recorded: `MD_HR` fix uses a whitespace-stripped alternation (the suggested backreference form is
  linear but `safe-regex` rejects star-height 2); one follow-up (`allowLanguageMismatch` on create) was fix-first, not red-first.
- e2e on this box now runs on SHIFTED ports (8190/8191/8092) because the starter's demo compose owns 8090/8091/4321;
  FORCE_SEED=1 on the dev DB gives a clean run. Two spec expectations had to follow deliberate behaviour changes (inline Form
  no longer offered; accessible grip name makes `getByRole(name)` need `exact`).
- Decisions left to the maintainer (flagged, not changed): top-level unknown-key passthrough → 422; default list page size;
  per-site ETag change counter; Dockerfile stage split/non-root; compose `cloudflared_default` network; session idle policy;
  value-schema strictness (LinkValue/ReferenceValue/BlockInstance); `useEffectEvent` refactor of the editor's message handler.
